pwd = input("What is the master password? ")

mode = input("Would you like to add a new password or view existing ones (type q to quit)? ").lower()

while True:
    if mode == "view":
        pass
    elif mode == "add":
        pass
    print("Invalid mode.")